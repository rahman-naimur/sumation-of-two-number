#include<stdio.h>
#include<conio.h>

int main()
{
    int a,b,sum;
    printf("Input one number:");
    scanf("%d", &a);
    printf("Input another number:");
    scanf("%d", &b);
    sum=a+b;
    printf("sum is %d", sum);
    getch();
    return 0;
}
